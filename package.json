{
  "name": "gol-logger",
  "version": "0.1.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "postbuild": "rm -f dist/tsconfig.tsbuildinfo",
    "prepublishOnly": "yarn run build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^4"
  }
}
